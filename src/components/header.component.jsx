import styled from "styled-components"

const HeaderContainer = styled.p`
    font-size: 32px;
`

const Header = () => (
	<>
		<HeaderContainer>pomodoro</HeaderContainer>
	</>
);
export default Header